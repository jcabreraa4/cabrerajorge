{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["COOKIE_DOMAIN", "ALLOWED_ORIGINS", "DATABASE_URL", "BETTER_AUTH_URL", "BETTER_AUTH_SECRET"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "start": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    }
  }
}
